{
  "_meta": {
    "note": "ANONYMIZED, English, k>=3-org distilled patterns from Binclusive's dynamic-audit corpus. Failure-shapes clustered offline by LLM (frozen in data/clusters/), gated k>=3-distinct-org and anonymized by deterministic code. Contains NO customer identifiers (org/project ids, elements, urls stripped; frequency as tier only). Regenerate by re-running the distiller.",
    "scope": ["2.4.1"],
    "corpusFindings": 647,
    "corpusOrgs": 27
  },
  "patterns": [
    {
      "id": "2.4.1-skip-link-broken",
      "wcag": ["2.4.1"],
      "component": "skip link target",
      "failureShape": "A \"skip to main content\" link exists but doesn't move focus when activated — focus stays on the link — so the bypass doesn't actually work.",
      "fix": "Point the skip link's href at the id of the main content container and give that target tabindex=\"-1\" so the browser moves focus into it on activation.",
      "frequencyTier": "occasional",
      "journeyTags": ["navigation", "browse-discovery", "form-submission"]
    },
    {
      "id": "2.4.1-skip-link-missing",
      "wcag": ["2.4.1"],
      "component": "skip link",
      "failureShape": "The page has no \"skip to main content\" link (or other bypass mechanism), so keyboard and screen-reader users must tab through the entire navigation/header on every page to reach the main content.",
      "fix": "Add a \"Skip to main content\" link as the first focusable element on the page, targeting the id of the main content region. Make it visible on focus.",
      "frequencyTier": "occasional",
      "journeyTags": ["browse-discovery", "checkout", "product-detail", "form-submission"]
    }
  ]
}
