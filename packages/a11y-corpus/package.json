{
  "name": "@binclusive/a11y-corpus",
  "version": "0.1.0",
  "type": "module",
  "description": "PRIVATE. The real-world audit corpus (the moat): distilled failure patterns, the frequency snapshot, and the distillation pipeline that produces them. Loaded by @binclusive/a11y at runtime when present; the OSS engine degrades to baseline-only coverage without it. Never published to public npm.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src",
    "data"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Binclusive/a11y-checker-plugin.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "distill": "tsx ./src/distill/run-distill.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "5.8.3",
    "vitest": "3.0.7"
  }
}
