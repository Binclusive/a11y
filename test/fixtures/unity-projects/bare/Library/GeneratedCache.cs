// A Unity-generated cache file under Library/ that incidentally mentions
// AccessibilityHierarchy. Library/ is a generated cache, NOT project source, so the
// scan must SKIP it — proving the bare project still fires no-screen-reader-support
// despite this string living in a non-source dir.
class GeneratedCache
{
    // AccessibilityHierarchy AccessibilityNode AssistiveSupport
}
