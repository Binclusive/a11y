// An app-OWN one-off component (imported as `~/components/widget`). It must
// never be suggested for the components map — only external design-system
// primitives are.
export function Widget() {
  return <div>widget</div>;
}
